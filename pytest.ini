[pytest]
asyncio_mode = auto
python_files = tests/unit/*.py tests/integration/*.py
python_functions = test_*
python_paths = src
addopts = --cov=src/services --cov-report=html --cov-report=term --cov-fail-under=85 --disable-warnings -q
markers =
    unit: Unit tests
    integration: Integration tests